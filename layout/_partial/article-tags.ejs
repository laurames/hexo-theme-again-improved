<% if (item.tags && item.tags.length){ %>
  <%
    var tags = [];
    item.tags.forEach(function(tag){
        tags.push('<a href="' + config.root + tag.path + '"></span>' + tag.name + '</a>');
    });
  %>
      <%- tags.join(', ') %>
<% } %>
